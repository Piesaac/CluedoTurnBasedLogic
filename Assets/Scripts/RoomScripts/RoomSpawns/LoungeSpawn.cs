using UnityEngine;

[System.Serializable]
public class LoungeSpawn : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public int id;
    public Transform spawnPoint;  
}
